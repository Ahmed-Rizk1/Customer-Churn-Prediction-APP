# ============================================================
#  Vercel-only requirements — API runtime deps only
#  DO NOT add notebook / visualization / frontend packages here.
#  Every extra MB counts — Vercel Lambda limit is 500 MB total.
# ============================================================

# FastAPI stack
fastapi==0.111.0
uvicorn==0.29.0
pydantic==2.7.1
python-multipart==0.0.9

# Data / model inference
numpy==1.26.4
pandas==2.2.2
scikit-learn==1.4.2

